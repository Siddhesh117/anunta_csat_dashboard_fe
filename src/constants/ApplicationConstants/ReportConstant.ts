const enum Pagination {
    INITIAL_PAGE = 1,
    PAGE_SIZE = 100,
    ALL_RESULT = -1
  }
  
  const enum TABLE_EXPORT {
    MAX_SIZE = 50_000,
    NOTIFICATION_DURATION = 5
  }
  
  export { Pagination, TABLE_EXPORT };
  